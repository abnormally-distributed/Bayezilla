model{
    df ~ dgamma(2 , .01)
    tau[1:P, 1:P] ~  dscaled.wishart(scale, df) 
    for (i in 1:P){
        mu[i] ~ dnorm(0, 1)
    } 
    for (i in 1:N) {
        X[i, 1:P] ~ dmnorm(mu, tau)
    }
  }
